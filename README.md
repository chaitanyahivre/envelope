Hivre Envelopes — simple static site

What I changed:
- Extracted inline CSS to `css/styles.css`.
- Added `js/main.js` for menu toggle and scroll reveal animations.
- Updated `index.html` to reference external assets and simplified inline JS.

Preview locally:

Open `index.html` in your browser, or run a simple static server:

Windows (PowerShell):

```powershell
# from project root
python -m http.server 8000
# then open http://localhost:8000
```
